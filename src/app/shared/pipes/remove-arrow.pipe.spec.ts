import { RemoveArrowPipe } from './remove-arrow.pipe';

describe('RemoveArrowPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveArrowPipe();
    expect(pipe).toBeTruthy();
  });
});
